# To test, concurrently
#   pnpm build && pnpm preview --host 0.0.0.0
#	  lychee dist src/content

base = "http://localhost:3000"
remap = [ "https://labs.leaningtech.com http://localhost:3000" ]

# Check links inside `<code>` and `<pre>` blocks
include_verbatim = true

exclude = [ '.*twitter\.com.*', '.*linkedin\.com.*' ]
